services:
  app:
    image: ${PROD_IMAGE:-ghcr.io/tuo-user/yii3-template}:${PROD_IMAGE_TAG:-latest}
    depends_on:
      db:
        condition: service_healthy
    environment:
      APP_ENV: prod
      APP_DEBUG: "false"
      SERVER_NAME: "${SERVER_NAME:-:80}"
      DB_DSN: "mysql:host=db;port=3306;dbname=${DB_DATABASE:-yii3_template}"
      DB_USERNAME: "${DB_USERNAME:-yii3_template}"
      DB_PASSWORD: "${DB_PASSWORD:?Set DB_PASSWORD in .env.prod}"
    networks:
      - app_internal
      - caddy_public
    volumes:
      - runtime:/app/runtime
      - caddy_data:/data
      - caddy_config:/config
    labels:
      caddy: ${PROD_HOST:-app.example.com}
      caddy.reverse_proxy: "{{upstreams 80}}"
    restart: unless-stopped

  db:
    image: mysql:8.4
    environment:
      MYSQL_DATABASE: "${DB_DATABASE:-yii3_template}"
      MYSQL_USER: "${DB_USERNAME:-yii3_template}"
      MYSQL_PASSWORD: "${DB_PASSWORD:?Set DB_PASSWORD in .env.prod}"
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:?Set MYSQL_ROOT_PASSWORD in .env.prod}"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - app_internal
    volumes:
      - db_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  runtime:
  caddy_data:
  caddy_config:
  db_data:

networks:
  app_internal:
  caddy_public:
    external: true
