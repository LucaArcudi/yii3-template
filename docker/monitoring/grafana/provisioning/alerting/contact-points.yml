# Contact point per le notifiche degli alert: bot Telegram.
# Token e chat ID arrivano dall'ambiente del container Grafana
# (docker/monitoring/.env, vedi .env.example): il provisioning di
# Grafana espande $VAR dai valori d'ambiente.
apiVersion: 1

contactPoints:
  - orgId: 1
    name: telegram
    receivers:
      - uid: telegram-ops
        type: telegram
        settings:
          bottoken: $TELEGRAM_BOT_TOKEN
          # Da Grafana 13.1 il receiver accetta correttamente l'intero prodotto
          # dall'espansione, sia per chat private sia per gruppi (ID negativo).
          chatid: $TELEGRAM_CHAT_ID
