[vps]
; Copia questo file in inventory.ini (ignorato da git) e inserisci i dati reali.
; La chiave privata deve essere quella autorizzata per l'utente di deploy sul VPS.
prod-vps ansible_host=<VPS_IP> ansible_user=deploy ansible_ssh_private_key_file=~/.ssh/<CHIAVE_PRIVATA>
